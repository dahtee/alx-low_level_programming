#include main.h
/**
 *print_sign - prints the signs of a number
 *@n: is int to be checked
 *Return: 0 if n is zero and - if n is less than 1
 */
int print_sign(int n)
{
	if (n > 0)
	{
		_putchar('+');
		return (1);
	}
	else if (n == 0)
}
